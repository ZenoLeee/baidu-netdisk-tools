# 百度网盘工具箱

一个简单易用的百度网盘管理工具，支持文件上传、下载、断点续传等功能。

## ✨ 主要功能

- **文件管理**: 浏览、上传、下载、删除、重命名百度网盘文件
- **断点续传**: 支持上传和下载的断点续传，网络中断自动恢复
- **分片上传**: 大文件自动分片上传，提高传输稳定性
- **多账号管理**: 支持保存多个账号，快速切换
- **拖拽上传**: 支持拖拽文件到上传列表
- **版本更新**: 自动检查更新，支持一键更新

## 📸 界面预览

### 现代化 UI 设计
- 简洁直观的用户界面
- 实时传输进度显示
- 支持深色/浅色主题

## 🚀 快速开始

### 环境要求

- Python 3.8+
- Windows 10/11

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行程序

```bash
python main.py
```

### 打包成 EXE

```bash
# Windows
build.bat
```

打包后的 EXE 文件位于 `dist/百度网盘工具箱.exe`

## 📦 依赖项

```
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0
requests>=2.31.0
qdarkstyle>=3.1.0
qt-material>=2.14.0
```

## 🎯 主要特性

### 文件传输
- **智能分片**: 根据文件大小自动选择最优分片策略
- **断点续传**: 支持上传和下载的断点续传
- **并发传输**: 多任务并发处理，提高传输效率
- **速度显示**: 实时显示传输速度和进度

### 账号管理
- **多账号**: 支持保存多个百度账号
- **快速切换**: 一键切换账号
- **自动登录**: 记住上次使用的账号
- **安全存储**: 账号信息本地加密存储

### 用户体验
- **拖拽上传**: 支持拖拽文件到程序窗口
- **右键菜单**: 完整的文件操作右键菜单
- **状态栏**: 实时显示传输状态和速度
- **进度条**: 清晰的进度显示

## 📝 配置说明

### 配置文件

程序首次运行会自动创建 `config.json` 配置文件，包含：
- 账号信息
- 下载目录
- 其他用户设置

### 下载目录设置

通过菜单 `设置 > 设置下载目录` 可以修改默认下载路径。

## 🔧 开发说明

### 项目结构

```
baidu-netdisk-tools/
├── core/                 # 核心功能模块
│   ├── api_client.py     # 百度网盘 API 客户端
│   ├── transfer_manager.py  # 传输管理器
│   ├── version_manager.py   # 版本管理器
│   └── constants.py      # 常量定义
├── gui/                  # 界面模块
│   ├── main_window.py    # 主窗口
│   ├── login_dialog.py   # 登录对话框
│   ├── transfer_page.py  # 传输页面
│   └── style.py          # 样式管理
├── utils/                # 工具模块
│   ├── config_manager.py # 配置管理
│   ├── file_utils.py     # 文件工具
│   └── logger.py         # 日志工具
├── static/               # 静态资源
│   └── *.qss             # Qt 样式表
├── main.py               # 程序入口
├── requirements.txt      # 依赖列表
└── README.md            # 项目说明
```

## 📮 联系方式

- 作者: Zeno
- GitHub: [@ZenoLeee](https://github.com/ZenoLeee)
